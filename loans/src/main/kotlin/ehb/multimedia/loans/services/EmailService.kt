package ehb.multimedia.loans.services

import ehb.multimedia.loans.dto.Email
import org.springframework.stereotype.Service
/*
@Service
class EmailService(val MailSender) {

    fun sendMail(email: Email) {
        val emailContent = createSimpleMessage(email)
        emailSender.send(emailContent)

    }

}*/